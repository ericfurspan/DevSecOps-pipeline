name: SAST — Semgrep

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  vulnerable-fixture:
    name: Verify expected Flask findings
    uses: ./.github/workflows/reusable-semgrep.yml
    with:
      target: app
      mode: fixture
      expected_findings: .semgrep/expected-vulnerable.json
